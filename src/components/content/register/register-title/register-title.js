import React from 'react';
import './register-title.css';

const RegisterTitle = () => {
  return (
    <h2 className="register-title">
      Clean <span className="register-title-span">Sign Up </span> Form
    </h2>
  )
};

export default RegisterTitle;